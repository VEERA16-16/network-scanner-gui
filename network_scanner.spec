# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['network_scanner.py'],
    pathex=[],
    binaries=[],
    datas=[('scan_results.json', '.'), ('scan_results.html', '.'), ('scan_results.csv', '.'), ('open_ports_chart.jpg', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='network_scanner',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['D:\\PROJECTS\\myicon.ico'],
)
